#ifndef TOOLS_ROUNDING_MODE_HPP
#define TOOLS_ROUNDING_MODE_HPP

namespace tools {
  enum class rounding_mode {
    ceiling,
    down,
    floor,
    half_down,
    half_even,
    half_up,
    up
  };
}

#endif
